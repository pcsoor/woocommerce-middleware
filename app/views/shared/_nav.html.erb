<nav class="bg-white border-b border-gray-100">
  <div class="mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      
      <div class="flex-shrink-0 text-lg font-medium text-gray-700">
        <%= extract_store_name(current_user.store&.api_url) || "My Store" %>
      </div>

      <div class="flex space-x-6 items-center">
        <%= link_to "Products", products_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
        <%= link_to "Categories", categories_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>

        <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-700 font-medium" %>
      </div>

    </div>
  </div>
</nav>
